@model IEnumerable<Positions>

@{
    ViewData["Title"] = "List of Positions";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container p-4">
    <h2>@ViewData["Title"]</h2>

    <div class="mb-3 mt-4">
        <a asp-controller="ApplicationRoles" asp-action="Create" class="btn btn-dark mb-4">Create New Role</a>
    </div>
    <table class="table table-striped">
        <thead>
            <tr class="bg-secondary">
                <th>Position Id</th>
                <th>Position Name</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.PositionId</td>
                    <td>@item.PositionName</td>
                    <td>
                        <a asp-controller="Positions" asp-action="Details" asp-route-id="@item.PositionId" class="btn btn-success">Edit</a>
                        <a asp-controller="Positions" asp-action="Edit" asp-route-id="@item.PositionId" class="btn btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
